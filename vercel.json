{
    "version": 2,
    "env": {
    },
    "builds": [
      {
        "src": "api/server.ts",
        "use": "@vercel/node"
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "api/server.ts"
      }
    ]
  }